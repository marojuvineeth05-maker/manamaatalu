# ManaMaatalu
Telugu-only random text/video chat MVP.

Files: index.html, style.css, app.js, server.js, package.json, README.md.

Run: npm install && npm start

Production needs HTTPS, TURN, age verification, moderation, reporting, rate limits, privacy policy and terms.